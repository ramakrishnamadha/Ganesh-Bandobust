const Database = require("better-sqlite3"); const db = new Database("dev.db", { readonly: true }); const tables = ["User","UserPoliceStationAccess","PreInstallationVerification","UserSession","DeviceSession","OfficerCurrentLocation","OfficerLocationHistory","ActivityEvent"]; for (const table of tables) { const row = db.prepare("SELECT COUNT(*) AS count FROM " + table).get(); console.log(table + ": " + row.count); } db.close();
